{
  "name": "task-manager-backend",
  "version": "1.0.0",
  "description": "Task Manager API with Node.js, Express, PostgreSQL, and Prisma",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prisma": "^5.8.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
